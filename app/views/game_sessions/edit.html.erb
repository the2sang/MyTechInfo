<% content_for :title, "운동 세션 수정" %>

<div class="cl-page">
  <div class="cl-content cl-content--form">
    <div style="margin-bottom:32px;">
      <p class="cl-hero__eyebrow">
        <%= link_to "운동 세션", game_sessions_path %>
        <span class="sep">/</span>
        <%= link_to @game_session.title, game_session_path(@game_session) %>
        <span class="sep">/</span> 수정
      </p>
      <h1 class="cl-hero__title">운동 세션 수정</h1>
    </div>
    <div class="cl-form-card">
      <%= render "form", game_session: @game_session %>
    </div>
  </div>
</div>
