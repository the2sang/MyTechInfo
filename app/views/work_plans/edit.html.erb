<% content_for :title, "작업계획서 수정" %>

<div class="page page--editor">
  <div class="page__header">
<%= link_to work_plan_path(@work_plan), class: "btn btn--ghost btn--sm" do %>
      <i class="fa-solid fa-arrow-left"></i> 상세로
    <% end %>
  </div>

  <%= render "form", work_plan: @work_plan, department_names: @department_names %>
</div>
